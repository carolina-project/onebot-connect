[package]
name = "bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
onebot-connect = { path = "../../onebot-connect", features = ["conn_full", "compat", "bridge"] }
tokio = { version = "1", features = ["rt"] }
eyre = "0.6"
log = "0.4"
env_logger = "0.11"
